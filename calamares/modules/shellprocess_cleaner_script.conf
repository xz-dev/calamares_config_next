---
# Set to true to run in host, rather than target system
dontChroot: true


script:
    - command: "/usr/bin/cleaner_script.sh"
      timeout: 240
      
i18n:
    name: "prepare post-installation cleanup tasks ..."
